#include "main.h"
/**
 * _islower - Program to check for lower case characters.
 * @c: the character to be checked
 * Return: 1 if char is lowercase, and 0 if it is not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
